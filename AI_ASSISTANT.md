# ผู้ช่วย AI ในเว็บ (Claude) — วิธีตั้งค่า

ปุ่ม 🤖 มุมขวาล่างของเว็บ = ผู้ช่วย AI สั่งงานเป็นภาษาไทยได้ เช่น
"สรุปรายรับวันนี้", "มีคิวอะไรค้างบ้าง", "ค้นหาแมวชื่อมีใจ", "เปิดคิวให้หมาชื่อบุ๋มของคุณนก ตรวจรักษา", "เพิ่มนัด HN 6900019 วันที่ 2026-07-01"

## ⚠️ ต้องตั้งค่า API key ก่อนถึงจะใช้ได้

ตัวผู้ช่วยคุยกับ Claude ผ่าน serverless function (`api/agent.js`) ที่ถือ API key ไว้ฝั่งเซิร์ฟเวอร์
(key **ไม่อยู่ในเบราว์เซอร์** จึงปลอดภัย) — ต้องใส่ key ใน Vercel:

1. ขอ API key จาก **Anthropic Console** → https://console.anthropic.com → Settings → API Keys → Create Key (ได้ค่าขึ้นต้น `sk-ant-...`)
2. ไปที่ **Vercel** → โปรเจกต์ opd-wangnoi → **Settings → Environment Variables** เพิ่ม:
   - `ANTHROPIC_API_KEY` = `sk-ant-...`  (จำเป็น)
   - `CLAUDE_MODEL` = `claude-opus-4-8`  (ไม่ใส่ก็ได้ — ค่า default. ถ้าอยากประหยัด/เร็วขึ้นใช้ `claude-haiku-4-5` หรือ `claude-sonnet-4-6`)
3. กด **Redeploy** (Deployments → ปุ่ม ⋯ → Redeploy) เพื่อให้ env ใหม่มีผล
4. เปิดเว็บ กดปุ่ม 🤖 แล้วลองพิมพ์ "สรุปรายรับวันนี้"

> ถ้ายังไม่ได้ตั้งค่า key ผู้ช่วยจะขึ้นข้อความเตือนให้ตั้งค่า — ส่วนอื่นของเว็บใช้งานได้ตามปกติ

## ผู้ช่วยทำอะไรได้บ้าง

**อ่าน/สรุป (ทำอัตโนมัติ):** ค้นหาสัตว์/เจ้าของ, ดูประวัติการรักษา, ดูคิว, สรุปรายรับ (วันนี้/7วัน/30วัน), ดูนัดหมาย, หาสินค้าใกล้หมด, เปิดหน้าเคส

**ทำงาน (ขึ้นการ์ดให้กด "ยืนยัน" ก่อนเสมอ):** เปิดคิวใหม่ / รับ walk-in, เพิ่มนัดหมาย, ยกเลิกใบเสร็จ

## ความปลอดภัย
- งานที่**แก้ข้อมูลจริง**จะมีการ์ดให้กดยืนยันทุกครั้ง ผู้ช่วยทำเองเงียบๆ ไม่ได้
- ผู้ช่วยไม่วินิจฉัยโรค/สั่งยาแทนสัตวแพทย์
- ค่าใช้จ่าย Claude คิดตามจำนวนข้อความ (ปกติถูกมากต่อคำสั่ง) — ดูยอดได้ใน Anthropic Console

## ค่าใช้จ่าย / การปรับแต่ง
- เปลี่ยนรุ่น AI: ตั้ง env `CLAUDE_MODEL`
- ปรับความฉลาด/ความเร็ว: แก้ `output_config.effort` ใน `api/agent.js` (`low` เร็ว/ถูก → `high` ฉลาด/ช้ากว่า)
- เพิ่มความสามารถ (เครื่องมือใหม่): เพิ่มใน `AGENT_TOOLS` + `runReadTool`/`runWriteTool` ใน `src/agent.jsx`
